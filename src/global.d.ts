// src/global.d.ts
declare class HSWebSocket extends WebSocket {
    constructor(url: string);
    // Add any specific methods or properties you expect here
    HSWebSocket();
  }
  
  declare class HSIWebSocket extends WebSocket {
    constructor(url: string);
    // Add any specific methods or properties you expect here
    HSIWebSocket();
  }